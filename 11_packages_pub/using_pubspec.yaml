# pubspec.yaml — example file

name: my_dart_project
description: A demo project using Dart packages
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.1.0
  path: ^1.9.0
  shared_preferences: ^2.2.1
